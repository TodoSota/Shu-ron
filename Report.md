# このプログラムについて
MPMにてエネルギーを計算し、粒子の位置を更新し、その結果を3DCGとしてレンダリングするプログラム。
MPMにおけるグリッド構造を"3Dテクスチャ"にて代替し、計算、近傍へのデータ転送、描画において使用することで全体のパフォーマンス向上を図れるのではと考えている。

~~ただし、コンパイルエラーこそないものの、画面内の粒子が動かないために、内部でのデータ転送や計算式に問題がある。~~

### **解消しました！！**

# 今回での変更
- データ構造体(SSBOでのデータ転送)は内部的にstd140にて行われるために、std430で宣言していても変数matやvecなどで確保されるバイト数で食い違うために、正しい数値保存がなされていない可能性があるために、構造体の変数宣言の見直しを行う必要があったためそこを修正。結果的にパディングという空変数が生まれたため、何か処理を追加したくなったらそこを利用する。
- main.cpp , mpmObject.h において、構造体の変数の型の見直し、それに伴うコンピュートシェーダーでの構造体の宣言の変更を行った。
- シミュレーションは動くようになったが、**挙動がおかしかった。**原因はグリッドから(グリッドへ)データ転送をする際のBスプラインなどの影響範囲となる基点の設定のオフセットの設定に問題があった。
- 以下はどちらも結局同じのように見えるが、以降の計算部分において、fx はグリッドでの座標、base はボクセルでの基準となる座標である(こっちを基準として扱う)。以降の計算においてこの少しのズレは計算空間を歪め物理的にあり得ない力場の形成に至っていた。

  (旧) vec3 fx = x * inv_dx - 0.5; ivec3 base = ivec3(floor(fx));

  (新) vec3 fx = x * inv_dx; ivec3 base = ivec3(floor(fx - 0.5));

<MPM計算における各段階でのコンピュートシェーダー>
- setup	: 変更なし。
- p2g 	: 構造体変更。基点位置の変更。
- grid	: 構造体変更。
- g2p	 : 構造体変更。基点位置の変更。
- move	: 構造体変更。

# 以降の方針
シミュレーションの描画領域は2×2の範囲であるのに、粒子の存在範囲はテクスチャに倣って0-1の1×1範囲のために結果が見づらく、また仮想の領域端が存在してしまっているので、視点の変更ないし粒子の存在範囲の拡大を行う。

処理の追加の前に、不要機能・変数の整理、プログラムのリファクタリングなどを行う。

**使用しなくなったファイルについても一応残しておくが、今後使用しなくなった場合には以下の紹介部分に沿うとわかるように示しておく。**

# プログラムの構成ファイル(ライブラリを除く)
### main,cpp

MPMにて計算を行う土砂シミュレーションシステム。
計算の本体は以下の[.comp]ファイルにて定義している。
<自力での追加分>
粒子の描画までの流れを流用し、床を表示するようにした。(床は点群にて表示しているので、高さをランダムにすれば山のような地形を作成できるカモ...)
既存の粒子シミュレーションの文も残しつつMPMに向けてコードを追加。

**現状MPMシミュレーションは稼働していると思われる**


### Window.h
ウィンドウ管理ライブラリのGLEWをラップし、
- ウィンドウ・ライフサイクル管理
- 仮想トラックボール
- GUIの統合
を行っている

### shader.h/.cpp
テキストファイルとして存在するシェーダーソースファイルを、プログラムオブジェクトへと変換している。
「頂点+フラグメント」の形式だけでなく、「コンピュートシェーダ」単品でのビルドにも対応している。
またビルド関数の内部にて、ファイルI/Oも入っているので、main側ではシンプルにファイルを渡すだけで良くなっている。

### Object.h/.cpp
シミュレーション領域における粒子の情報を、GPUのビデオメモリ上にて一括管理するための処理を行っている。
SSBO (Shader Storage Buffer Object)としての使用も行うので、速度と位置などを入力している。
現在は粒子物理シミュレーションのため、位置(x)、速度(v)、力(f)を管理していて、プログラムの値の管理もそこで決め打ちしている。
**床面表示のために使用しているのそのまま残しているが、シミュレート用ではなくなったので速度などは不要**

## MPM シミュレーションに関連するコード
### <自力追加> mpmObject.h/.cpp
1つのシミュレーション領域に必要な全リソースをパッケージ化したもの。そのためグリッドにあたる3Dテクスチャと粒子のためのvao/vboを保持している。
粒子のプロパティとグリッドのプロパティを同時に保持している。粒子のものはフィールドとして存在している。
既存のObjectと競合するため構造体の名前を変更している。
構造体についてはコンピュートシェーダーでのデータ転送の仕様上、中身もなく処理にも利用されない変数も含まれている。

なお3Dテクスチャは、
- 補間を禁止するため GL_TEXTURE_MIN_FILTER / MAG_FILTER を GL_NEARST に設定
- 範囲外アクセス(境界端でのアクセス)時に端の値を固定するため GL_TEXTURE_WRAP_S / T / R を GL_CLAMP_EDGE に設定
している

### <自力追加>mpm_setup.comp
グリッドである3Dテクスチャの初期化。前回でのデータが残ることによりシミュレーションに影響を与えないようにする。

### <自力追加>mpm_p2g.comp
MPMにおける、P2G(Particle to Grid)の段階を担当する。細かい処理についてはコードを確認されたい。
GPUで並列計算され、その結果が4枚のテクスチャに一気に書き込まれるので、安全な書き込みのために、AtomicAddをCAS（Compare-And-Swap）ループにて実装している。
計算上必要であり、かつGLSL内の関数しか使用できないので、ヘルパー関数として固有値分解と特異値分解を実装している。

### <自力追加>mpm_grid.comp
グリッドにおける(接触判定と境界条件による)速度の更新を担当する。境界条件の処理も行うため、外部の物体との接触判定を実装したい場合はここに追加する。

### <自力追加>mpm_g2p.comp
MPMにおける、G2P(Grid to Particle)の段階を担当する。細かい処理についてはコードを確認されたい。
グリッドでの結果を戻し、それを使って一つ一つの粒子に対して各種ステータスの更新を行う。
なお通常、グリッドから粒子へ結果を戻す際には近傍粒子の探索を行わねばならないが、テクスチャであればimageLoad一つで可能となっている)
変形勾配などの値の算出が済んだ際、**塑性変形**や**硬化状態**などの材料特性の反映を行っている。
こちらも計算上必要であったのでヘルパー関数として固有値分解と特異値分解を実装している。

### <自力追加>mpm_move.comp
先の段階で粒子に対して返した結果を用いて、最終的な粒子位置の更新を行う。

~~基本的に[mpm_grid.comp]において接触判定を行うことができるが、念のため粒子単位でも粒子位置に対して境界条件の判定を行っている。~~

↑ やはり不要だったのでコメントアウトしている

## 描画される点におけるバーテックスシェーダーとフラグメントシェーダー
### point.vert
取り出した位置をMVP行列を掛け合わせて、画面という2D上の位置に合わせて計算する。
計算している粒子と床面にて計算を変えている。床面の時のみ、シミュレーション中に傾けられるように計算を行っている。

### point.frag
描画するピクセルの色を指定しているだけ。
床と粒子で色を変えている

## <未使用>粒子ベースシミュレーションのコンピュートシェーダー
### setup.comp
シミュレーションの最初ステップにて初期化処理を行っている。
力は引き継いでいるとまずいので、こちらだけ初期化して、速度や位置などは引き継いでいる。

### update.comp
シミュレーションの中核ステップ。運動方程式を解いて力を求め、粒子を動かしている。
時間積分の計算と、簡易的な境界条件を実装している。

### collide.comp
DEMに近い計算により衝突判定を行っている。
「全対全」の総当たり探索を行っていて、O(N^2)の計算量である。

